import 'package:flutter/material.dart';

class HomeRelatedPlants extends StatelessWidget {
  const HomeRelatedPlants({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('...');
  }
}
